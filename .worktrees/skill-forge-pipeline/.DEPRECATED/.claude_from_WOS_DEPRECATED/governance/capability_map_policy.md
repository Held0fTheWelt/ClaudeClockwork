# Capability Map Policy (v17.6)

## Goal
Provide a deterministic snapshot of what the clockwork can do.

## Tool
Use `capability_map_build` to generate a JSON map (skills/agents/contracts/governance/tasks).

## Usage
- attach to evidence runs
- use for onboarding and audits
