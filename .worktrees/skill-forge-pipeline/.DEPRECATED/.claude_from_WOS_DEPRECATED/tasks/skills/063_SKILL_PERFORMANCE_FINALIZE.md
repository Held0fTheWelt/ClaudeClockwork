# Skill Task: performance_finalize

## Intent
Run budgeting outputs at end of process using config defaults.

## Acceptance
- Reads config and executes budget report export
- Optional efficiency review merge when panels provided
- Can auto-disable budgeting when token threshold exceeded
