# Skill Task: model_routing_record_outcome

## Intent
Update routing stats (hit list) based on outcome.

## Acceptance
- deterministic
- append/update stats in `.claude/brain/model_routing_stats.json`
