# Skill Task: efficiency_review

## Intent
Merge an LLM panel review (worker/teamlead/judge) with the budget report.

## Acceptance
- deterministic: does not call LLMs
- outputs review JSON/MD + optional chart export
- CLI summary contains clear attribution and scores
