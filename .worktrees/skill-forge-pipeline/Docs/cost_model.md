# Cost Model (Phase 41)

- **expected_latency_ms**, **expected_gpu_minutes**, **expected_memory_mb**, **expected_failure_risk**
- Defaults and deterministic estimation. Same inputs → identical estimates.
