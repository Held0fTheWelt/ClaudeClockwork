# Plugin Compatibility (Phase 40)

- Plugin manifest declares `clockwork_compat` (e.g. ">=17,<19").
- Compatibility matrix: Clockwork version ↔ plugin versions. Incompatible plugin rejected with clear error.
