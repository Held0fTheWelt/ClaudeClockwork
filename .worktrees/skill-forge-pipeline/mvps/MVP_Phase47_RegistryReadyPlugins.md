# MVP Phase 47 — Community/Registry Ready (Optional Distribution Track)

**Goal:** Make plugin distribution and discovery registry-ready while staying repo-local by default: publish workflow, validation pipeline, signing defaults, and an index format that can be mirrored later.

---

## Definition of Done

- [x] Registry index format exists
- [x] Publish workflow exists (test → sign/hash → index update)
- [x] Bundle validator exists
- [x] Docs exist for authors/operators
- [x] CI validates plugin submission bundles deterministically
- [x] All existing tests pass

---
