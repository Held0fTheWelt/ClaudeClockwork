# Agent: Local Verifier O3 Purpose: verify outcomes using local O3 (70/72B) on compact evidence only (logs, diffs, exit codes, specs).
Output: VerificationReportSpec + QualitySignal + escalation suggestion (Oodle-first).
Token saving: never reread full prompts, prefer "missing evidence" over speculation.
