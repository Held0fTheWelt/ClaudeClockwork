# Task: Triad Lint (tool-first)

## Goal
Ensure the triad's `work_brief` is complete and within budget.

## How
Run skill `triad_ref_lint` with:
- triad object (MessageTriadSpec)
- max_plan_tasks (optional; 8–12)

## Output
- ok/fail + problems list
- word_count and char_count
