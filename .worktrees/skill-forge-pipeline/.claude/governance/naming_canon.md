# Naming Canon

To avoid drift in terminology:

- **Skill Scout**: silent observer that proposes skill candidates (max 3).
- **Skill Planning Agent**: turns an approved candidate into an implementation plan (TasklistSpec + acceptance).
- **Skill Agent (learning)**: archival / meta reference only; not an operational entrypoint.
