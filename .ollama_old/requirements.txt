# .ollama — Standalone Task Execution Service
# No external dependencies required for the service itself.
#
# TaskExecutorService imports claudeclockwork lazily and handles ImportError gracefully.
# If claudeclockwork is not installed, service returns clear error messages.

# Development/Testing only
pytest>=7.0
pytest-cov>=4.0
requests>=2.28  # For manual Ollama/Claude testing
