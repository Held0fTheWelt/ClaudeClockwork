# Orchestrator Mode (Phase 43)

- `orchestrate run --workspace W --project P --graph G`
- `orchestrate incident --last`, `orchestrate export-incident`, `orchestrate import-bundle`
- Commands respect boundaries; output deterministic.
