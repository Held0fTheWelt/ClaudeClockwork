# Plugin Test Harness (Phase 40)

- Plugins must ship: smoke test definition (manifest-declared), policy compliance check.
- Runner executes these deterministically. Plugin without tests can be rejected or warned by policy.
