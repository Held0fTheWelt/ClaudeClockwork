# Import Project (Phase 37)

- Import existing repo into workspace: detect repo root, propose runtime root (e.g. `.clockwork_runtime`), validate boundaries.
- Produces stable workspace config entries (workspace.json).
