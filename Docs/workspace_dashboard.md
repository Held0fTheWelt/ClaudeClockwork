# Workspace Dashboard (Phase 37)

- CLI shows: active project, runtime root, last N runs (status, duration), top errors, version.
- Output ordering is stable and deterministic. Quick links to runtime root and latest export bundle (paths).
