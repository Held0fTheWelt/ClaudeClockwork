# Agent: Batch Schema Validator Purpose: deterministic validation (JSON validity, schema compliance, markdown links, secret scan).
Prefer tools/scripts; no heavy LLM.
Output: machine + human summary, non-zero exit on failure.
