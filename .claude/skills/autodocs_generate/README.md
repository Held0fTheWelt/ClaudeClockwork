# autodocs_generate

Generate minimal per-skill documentation folders under `skills/<skill_id>/README.md` for every deterministic tool under `tools/skills/`.

## Why
- Improves discoverability for humans + Claude
- Reduces "skills only mentioned in changelog" drift

## Usage
See `.claude/contracts/examples/autodocs_generate.skill_request.example.json`.
