# performance_toggle

Enable/disable performance budgeting (token cost reporting) at runtime.

## Why
- Budgeting is **enabled by default**.
- The TeamLead can disable it when the luxury is too expensive.
- It must also be toggleable by explicit instruction to Claude Code.

## Skill ID
- `performance_toggle`
