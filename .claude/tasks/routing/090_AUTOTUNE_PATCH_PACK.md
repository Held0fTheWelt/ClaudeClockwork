# Task: Autotune Patch Pack (approval-gated)

## Goal
Convert RouteAutotuneSuggestion into patch files (RouteProfileSpec) for manual approval.

## How
Run `route_profile_patch_pack` with the suggestion output from v13.

## Output
- patch files under `.claude/proposals/route_profile_patches/`
