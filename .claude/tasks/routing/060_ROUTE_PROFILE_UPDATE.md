# Task: Update Route Profiles (Personal Best Routes)

## Goal
Record the best-known cheap routing for common task types.

## Input
- One RouteProfileSpec (task_type + preferred doer/verifier/relay + fallbacks)

## Output
- Updates `.claude/knowledge/route_profiles.json` via `route_profile_update` skill
