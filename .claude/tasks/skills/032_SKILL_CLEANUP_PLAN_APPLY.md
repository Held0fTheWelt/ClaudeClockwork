# Skill Task: cleanup_plan_apply (Apply Cleanup Plan)

## Intent
Safely apply a cleanup plan generated by `repo_clean_scan`.

## Safety requirements
- Must support `dry_run=true`
- Must disable delete by default (`allow_delete=false`)
- Must refuse operations outside `root`

## Inputs
See `.claude/skills/cleanup_apply/README.md`

## Outputs
- cleanup_apply_report.json
