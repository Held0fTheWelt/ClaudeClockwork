capabilities:
  embed.text:
    runner: embed
    tool_id: embed_text
    description: Local text embeddings
  audio.asr:
    runner: asr
    tool_id: audio_asr
    description: Local speech-to-text (ASR)
  code.forge:
    runner: forge
    tool_id: code_forge
    description: Generate code from architecture plan
  code.review:
    runner: review
    tool_id: code_review
    description: Static analysis of generated code
  code.validate:
    runner: validate
    tool_id: code_validate
    description: Deterministic safety validation gates
