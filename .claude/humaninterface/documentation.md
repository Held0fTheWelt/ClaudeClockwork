# Documentation Rules

- Docs live in `Docs/` (or `.claude/knowledge/` for agent-internal references).
- Every new doc must state:
  - Purpose
  - Scope
  - Owner (agent/department)
  - Last updated date
- Avoid duplicating canonical rules; link to them instead.
