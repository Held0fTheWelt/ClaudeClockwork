# Recovery Playbooks (Phase 39)

- Retry with backoff (worker retry policy).
- Reroute node to local worker (dispatcher fallback).
- Bypass cache and recompute (workgraph resume=False).
- Recovery actions captured in telemetry.
